services:
  - type: web
    name: nirvaha-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python -m backend.seed.seed_data"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.9
